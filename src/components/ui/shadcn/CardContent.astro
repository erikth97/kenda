---
/**
 * CardContent.astro
 * 
 * Componente para el contenido principal de la tarjeta
 * Basado en el diseño de shadcn/ui
 */

 interface Props {
  className?: string;
  padding?: boolean;
}

const { 
  className = '',
  padding = true
} = Astro.props;

const classes = [
  padding ? 'px-6 py-4' : '',
  className
].filter(Boolean).join(' ');
---

<div class={classes}>
  <slot />
</div>