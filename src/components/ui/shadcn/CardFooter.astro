---
// CardFooter.astro - Componente para el pie de tarjeta
import { cn } from "../../../lib/utils";

export interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...props } = Astro.props;
---

<div class={cn("flex items-center p-6 pt-0", className)} {...props}>
  <slot />
</div>